# Yendou

## Getting Started

Install dependencies and start the dev server:

```sh
npm install
npm run dev
```

Vite will print the local URL in the terminal, usually:

```txt
http://localhost:5173/
```

Open that URL in your browser to review the app.
